# Issue #160 — Status

- [ ] Done

## What's done

- Plan accepted (recommended defaults: Phase 1 code + design doc; hard-refuse
  remotes when served; docs-only credentials). Q3 already decided (filefinder = phase 2).
- `/iflow-build` started on `cursor/160-otherpath-remote-b1a0`.
- PR: https://github.com/cellpy/cellpy-simple-gui/pull/161 (#161, draft)

## Remaining work

- Implement Phase 1: remote URI helpers, `expand_paths` policy, adapter loads,
  UI/docs/design note, tests.
- File follow-up issue for Phase 2 (filefinder) at close if not already open.

# Issue #73 status

- [ ] Done

## What's done

- Plan accepted (ellipsis + `:title` hover).
- CSS: `.job-msg` overflow clamp (mirror `.proj-tag`).
- HTML: `:title` on `.job-msg`.
- Design note in `job-cancel-dismiss.md`.

## Remaining work

- Run `uv run pytest`; commit / push; close.

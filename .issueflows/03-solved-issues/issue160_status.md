# Issue #160 — Status

- [x] Done

## What's done

- Plan accepted (Phase 1 code + design doc; hard-refuse remotes when served;
  docs-only credentials; Q3 = filefinder as phase 2).
- Phase 1 implemented: remote URI helpers, `expand_paths` policy, adapter
  `load_file`/`load_raw`, UI hints, README + deployment notes, design doc,
  unit tests. Full `uv run pytest` green.
- Phase 2 filed as a follow-up GitHub issue (see close notes).
- PR: https://github.com/cellpy/cellpy-simple-gui/pull/161

## Remaining work

- None for phase 1 / this issue.

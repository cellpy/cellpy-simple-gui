# Issue #60 status

- [x] Done

## What's done

- Apply `y_ranges` once on the merged `figures_json` base (hover map + title fallback).
- Stop forwarding `y_ranges` into every multi-part `collection.plot`.
- Regression test for charge + CE ranges on mixed group-avg path (no facet-miss warn).
- Design note updated in `summary-independent-y-scales.md`.

## Remaining work

None.

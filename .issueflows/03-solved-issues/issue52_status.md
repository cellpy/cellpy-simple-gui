# Issue #52 — Status

- [x] Done

## What's done

- Plan accepted (pin ≥2.1.1.post4; ingest schema wrapper-only; layout via layout_updates; keep group-avg partition).
- Bumped `cellpy` to `>=2.1.1.post4`.
- Inventory: `.issueflows/04-designs-and-guides/cellpy-delegation-inventory.md`.
- Delegated: quiet `list_instruments`; `_inject_app_chrome` → #801 knobs; pretty labels; facet remap via hover `variable=`.
- Wrappers: `read_file_meta`, `instrument_meta_schema` (UI wiring deferred).
- Docs: `CELLPY_PAINPOINTS.md`, `this-project.md`, `plot-appearance.md`.
- Tests green (`uv run pytest`).

## Remaining work

- None for this issue (follow-ups documented in inventory).

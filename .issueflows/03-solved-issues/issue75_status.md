# Issue #75 status

- [x] Done

## What's done

- Plan accepted (auto-load on folder pick; filename/dir detect; always-show Open row).
- `classify_import_path` + `resolve_project_path` accepts `project.json` file; `POST /api/projects/classify-import`.
- Open row always visible + refresh; import path opens project or journal; folder pick `kind=folder`.
- Tests in `test_projects.py`; design notes updated.
- `uv run --extra dev pytest` green.

## Remaining work

- None (ship via PR).

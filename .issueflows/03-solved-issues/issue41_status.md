# Status — Issue #41

- [x] Done

## Progress

- `load_raw` passes `auto_pick_cellpy_format=False` with the selected instrument.
- Clearer error when a `data_df` native-reader miss occurs.
- Load-cells UI hint: Arbin SQL HDF5 → Import raw.
- Mock tests for kwargs + error rewrite; instruments list includes `arbin_sql_h5`.

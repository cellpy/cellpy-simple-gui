# Issue #5 status

- [ ] Done

## What's done

- Copied SVG/PNG into `web/static/img/`; generated multi-size `.ico`.
- Header uses cellpy SVG; favicon replaced; `desktop.py` passes icon to `webview.start`.
- Tests for static branding paths + index logo.

## Remaining work

- Re-run pytest; close yolo (commit / PR / merge).

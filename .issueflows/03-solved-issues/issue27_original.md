# Issue #27: Iterative fixes: group average checkbox

Source: https://github.com/cellpy/cellpy-simple-gui/issues/27

## Original issue text

## Interactive `/iflow-fix` session

This issue tracks an interactive iterative-fix session. Individual fixes are recorded in the local status markdown under `.issueflows/` and landed together via `/iflow-close`.

### First reported bug

**Group avg** checkbox appears checked but the summary plot still shows per-cell traces instead of group averages (spread / share y-scale controls nearby).

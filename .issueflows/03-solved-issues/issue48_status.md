# Issue #48 — status

- [x] Done

## What's done

- Optional `e2e` extra (`playwright`) + pytest marker.
- `tests/test_gui_playwright.py`: live `ServerThread` + Chromium smoke
  (shell load; Load demo cells → cell cards → summary Plotly).
- Docs: README, `this-project.md`, `playwright-gui-tests.md`.
- `uv run pytest`: 78 passed (e2e when Chromium present; skip otherwise).

## Remaining work

- None.

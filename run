#!/usr/bin/env bash
# Launch cellpy-simple-gui from the repo root.
# Usage: ./run [--server] [--no-open] [--port N]
set -euo pipefail
cd "$(dirname "$0")"
exec uv run --extra export cellpy-simple-gui "$@"
